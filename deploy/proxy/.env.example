# Modelo do /opt/dscproject/proxy/.env na VPS (sem segredo, mas fora do git por padrao)
PROD_DOMINIO=dscproject.com.br
HML_DOMINIO=hml.dscproject.com.br
# Recebe avisos do Let's Encrypt (expiracao, problemas de emissao)
ACME_EMAIL=
